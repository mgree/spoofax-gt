core

  (\x : ?. if x : ? => bool then
    false
  else
    true) ((\y : ?. y) : ? -> ? => ?) : bool

evaluates to

  stuck (\x : ?. if x : ? => bool then
    false
  else
    true) ((\y : ?. y) : ? -> ? => ?) : bool with